- Prática com resolução de problemas sobre o assunto.

- **Missão:** Criar um script que receba um Array de objetos simulando dados (ex: uma lista de alunos com notas). Os alunos devem utilizar .map() para gerar blocos de HTML dinamicamente e injetá-los no DOM, e .filter() para exibir apenas alunos aprovados.